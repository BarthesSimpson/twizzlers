/* just for browser, not for nodejs */
// import Twizzlify from 'twizzlify'
// const os = require('os')
// const cpuCount = os.cpus().length

// export function twizzlify(
//   func,
//   partitionFunc = inferPartitionFunc,
//   collectFunc = inferCollectFunc,
// ) {
//   return (...args) => {}
// }

export default () => ({})
